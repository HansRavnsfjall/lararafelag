using Umbraco.Cms.Web.Common.PublishedModels;

public static class DevModelsSmokeTest
{
    // If this file compiles, the language server can see your generated models.
    public static string? Check(TopSection? model) => model?.Yvirskrift;
}
